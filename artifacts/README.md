# Artifacts

This directory is the standard local location for generated experiment artifacts.

Large binary artifacts are intentionally not tracked by ordinary Git. Train the
models with the scripts documented in the root `README.md`, or copy existing
checkpoints into the expected subdirectories.

```text
artifacts/
  models/      Trained model checkpoints, training histories, and metadata.
```

